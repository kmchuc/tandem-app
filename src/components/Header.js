import React from 'react';

// header component

const Header = () => {
  return (
    <header className="Header">
      <h1>it's about to get trivial</h1>
    </header>
  );
}

export default Header;